// A basic C program consits of the following:
	// Preprocessor Commands
	// Functions
	// Variables
	// Statements & Expressions
	// Comments

#include <stdio.h>

int main() {

	printf("Hello, World! \n");

	return 0;
}

// What does the aboe code do?

	// The first lin (#include <stdio.h>) is a preprocessor command, this tells a C compiler
	// to include stdio.h file before going to acula compilation.

	// int main(), this is where the program execution begins

	// printf(...), is a function in C that causes the message inside the function to display on screen

	// return 0; , this terminates the main() function and returns the value 0;